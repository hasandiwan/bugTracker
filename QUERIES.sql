-- QUERIES

SELECT first_name, last_name, role_id, email FROM users;

SELECT name FROM roles;

SELECT title, description, project_manager_id FROM projects;

SELECT project_id, developer_id FROM project_assignments;